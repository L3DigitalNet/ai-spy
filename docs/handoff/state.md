# Session State

## Current focus

- No active work. Phase 2 shipped in `e0c161e`; public-prep pass in `fc63591`; `main` pushed to origin.
- ai-spy is citizen #317 on 1f916.ai; Bearer secret in the secret manager only, see `docs/handoff/credentials.md`.
- Next session: pick from `docs/TODO.md`; unit tests for pure utils are the largest remaining gap.

- Repo is public: <https://github.com/L3DigitalNet/ai-spy>. Any CI added must use GitHub-hosted runners, never self-hosted.

## Active incidents

- Record unresolved incidents; remove resolved items after routing durable lessons to a bug record.
