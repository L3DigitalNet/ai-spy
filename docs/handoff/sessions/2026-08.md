# Session Log — 2026-08

What each development session did, appended by the AI agents that did it. History, not current truth: read [../../STATUS.md](../../STATUS.md) for that.

## 2026-08-06

- Scaffolded and implemented the ai-spy observer UI (`web/`): Vite + React 19 + TypeScript, Zod-validated API client, five hash-routed views.
- Views: feed (top/new), thread with depth-indented comments, citizens census, transparency (attestation, identity events, addresses), treasury.
- Verified the app against the live 1F916.ai API and in a browser via the dev proxy; nothing was committed, pending orchestrator verification and commit.
- Updated README.md, docs/STATUS.md, docs/TODO.md, and docs/handoff/architecture.md to reflect the implemented UI.
- Follow-ups routed to docs/TODO.md: util tests, eslint-plugin-react-hooks, single-gate wiring, Prettier html/css, live-updates view, untested branches.

## 2026-08-06 (phase 2)

- Registered ai-spy as 1F916.ai citizen #317 (handle `ai-spy`); Bearer secret lives only in the secret manager, path in `credentials.md`.
- Added the authenticated observer tier: `#/account` view, a scoped proxy header injection for `/api/me*` only, and a setup card when unconfigured.
- Added live-updates polling (`GET /api/changes`, 60s, visibility-aware) with a manual-refresh banner on the feed views.
- Added transparency-view witness mode: localStorage-saved chain heads, rechecked via `identity_from`/`identity_expect` (and `ledger_*`) on later visits.
- Added a `#/humans` view for the forum's `humans.txt`, linked from the footer.
- Restyled the app as a dark-only "observation deck": near-black palette, teal accent, spaced-mono chrome, sans-serif content; no light variant.
- Verified against the live API and a browser; nothing committed, pending orchestrator verification and commit.
- Updated README.md, docs/handoff/credentials.md, docs/STATUS.md, docs/TODO.md, and docs/handoff/architecture.md for phase 2.
- Follow-ups routed to docs/TODO.md: proxy header-scoping test, witness export/compare, StrictMode dev caveat, archive view (then still proposed).

## 2026-08-06 (phase 2 completion)

- Built the archive (`#/archive`): local index drained from `GET /api/changes?since=0`, dedup by id, lazy per-row hydration, two-tier cache.
- An adversarial verifier found a path-traversal defect in the proxy's credential scoping: raw-URL matching vs. normalized-path forwarding leaked the Bearer token.
- Fixed structurally: scoping now happens in a `proxyReq` hook on the final wire path, behind two fail-closed gates; any inbound `Authorization` header is stripped first.
- Added the repo's first automated test suite, `web/vite.config.test.ts` (109 cases), as the regression guard for that boundary; wired into `npm run check` via a new `npm test`.
- Residual documented honestly: the guard lives only in the dev/preview proxy, so a production host serving `web/dist/` must reimplement it.
- Verified against the live API and a browser; nothing committed, pending orchestrator verification and commit.
- Updated README.md, docs/STATUS.md, docs/TODO.md, and docs/handoff/architecture.md for the correction and the security fix.
- TODO's archive-proposal item removed (built); proxy header-scoping test item resolved; added production-host guard reimplementation as a follow-up.
