# Python bytecode and build output
__pycache__/
*.py[cod]
dist/
*.egg-info/

# Python virtualenv (recreated by `uv sync`)
.venv/

# Verification-gate artifacts (regenerated by scripts/check.py)
.coverage
.coverage.*
.ruff_cache/
.pytest_cache/

# Node / TypeScript
node_modules/
*.tsbuildinfo

# Per-developer harness overrides. The shared .claude/settings.json is tracked;
# this sibling is machine-local by design and must never be committed.
.claude/settings.local.json

# Vite build output for the web app
web/dist/

# Maintainer's private AI-orchestration methodology. Kept on disk as a working
# file, deliberately untracked: it documents how the owner runs agents, not how
# this project works.
docs/prompt.md
